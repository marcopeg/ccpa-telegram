## Question
Which defaults should the docs reflect for Antigravity — **`approvalMode: "yolo", sandbox: false`** (match code + Antigravity doc) or **`approvalMode: "default", sandbox: true`** (match `reference.yaml`, implying code would be wrong)?

## Answer
The docs should reflect the current implementation

## Source
docs/config/reference.yaml (engine.antigravity defaults); docs/engines/antigravity/README.md (Antigravity-specific config table)
