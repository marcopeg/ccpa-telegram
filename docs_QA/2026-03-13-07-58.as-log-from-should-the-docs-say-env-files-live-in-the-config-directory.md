## Question
Should the docs say env files live in the **config directory** (default: current working dir, or `--config-dir` if set), or should we keep “directory where you run the CLI”?

## Answer
Good catch. Update saying the env lives next to the config file now. Add a draft task to change “config-dir”
 Just “config” as parameter that looks better imho

## Source
docs/setup-wizard/README.md — Config directory and format
