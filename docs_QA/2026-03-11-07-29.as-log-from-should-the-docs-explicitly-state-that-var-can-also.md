## Question
Should the docs explicitly state that `${VAR}` can also be resolved from the shell environment, with values from `.env`/`.env.local` (or custom `env`) taking precedence?

## Answer
Yes.

.env.local -> .env -> process.env

## Source
docs/config/README.md (Environment variable substitution)
