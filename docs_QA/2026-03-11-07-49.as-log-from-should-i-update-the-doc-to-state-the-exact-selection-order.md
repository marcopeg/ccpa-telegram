## Question
Should I update the doc to state the exact selection order: **custom `env` if configured; else `.env` if it exists; else `.env.local` if it exists; otherwise create `.env`**?

## Answer
Yes

## Source
docs/setup-wizard/README.md (Config directory and format → Secrets)
