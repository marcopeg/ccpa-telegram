I want to support python commands and a .venv for each project.
I want each project to be able to expose its `requirements.txt` and I want the CLI to be able to setup the venv automatically, but placing the venv files in `~/.telegrapp/{project.slug}/.venv`.