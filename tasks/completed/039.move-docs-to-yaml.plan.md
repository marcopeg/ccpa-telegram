# Plan: 039 — Move docs to YAML

**Task:** [039 — Move docs to YAML](./039.move-docs-to-yaml.md)

## Overview

1. Create the canonical config reference and ensure examples are YAML-only.
2. Add the Configuration alternatives section and convert all doc snippets to YAML.
3. Update README, skills, and do a final review.

---

## Phase 1 — Reference config and examples/

**Goal:** Add `docs/config/reference.yaml` (all keys + doc links) and make `examples/` YAML-only with a short `hal.config.yaml`.

### Step 1.1 — Create `docs/config/reference.yaml` ✅

- **Done.** Created `docs/config/reference.yaml` with all top-level and nested keys (globals, providers, context, projects), comments linking to [Context](context/README.md), [Commands](commands/README.md), [Logging](logging/README.md), [Rate limit](rate-limit/README.md), [Engines](../engines/README.md), Codex, Antigravity. Placeholder values and one example project entry.
- Add a new file `docs/config/reference.yaml` that acts as the **config-key index** for the docs.
- Include every supported top-level and nested key from the config schema:
  - **Top-level:** `globals`, `providers`, `context`, `projects`
  - **globals:** `access`, `engine` (name, command, model, session, sessionMsg, codex, antigravity), `logging`, `rateLimit`, `transcription`, `dataDir`, `commands`
  - **Per-project:** same overridable keys (telegram, access, engine, providers, logging, rateLimit, transcription, dataDir, context, commands) plus `name`, `active`, `cwd`
- Use YAML comments above or beside each section to **link to the relevant doc page** (relative links from `docs/config/`), e.g.:
  - `# See [Context](context/README.md)`
  - `# See [Commands](commands/README.md)`
  - `# See [Engines](../engines/README.md)`, [Rate limit](rate-limit/README.md), [Logging](logging/README.md), [Access](#access-control in config/README), etc.
- Use placeholder or example values (e.g. one or two projects) so the file is valid and copy-pasteable; comments can document defaults and gotchas.
- Reference: `src/config.ts` Zod schemas (`GlobalsFileSchema`, `ProjectFileSchema`, `MultiConfigFileSchema`) and existing tables in `docs/config/README.md` for key names and descriptions.

### Step 1.2 — Ensure `examples/hal.config.yaml` exists as short subset ✅

- **Done.** Created `examples/hal.config.yaml` with globals (engine, logging, rateLimit, access) and two projects (backend, frontend); comment points to reference.yaml.
- If `examples/hal.config.yaml` does not exist, create it. If it exists, ensure it is a **shorter, copy-paste-friendly** subset (e.g. globals + 2 projects with common keys only), not the full key list. The single source of truth for "every key" is `docs/config/reference.yaml`.
- Ensure the file parses as valid YAML and is consistent with the loader expectations (project map, required telegram.botToken, access, etc.).

### Step 1.3 — Remove JSON/JSONC from `examples/` ✅

- **Done.** Removed `examples/hal.config.jsonc`. No other JSON/JSONC example files present in examples/.
- Delete or remove from version control any `hal.config.json`, `hal.config.jsonc`, or `_hal.config.jsonc` in `examples/` so the folder contains **only YAML** example(s).
- Do not add new JSON/JSONC example files in `examples/`.

---

## Phase 2 — Configuration alternatives section

**Goal:** Add a dedicated section in the config docs for JSON/JSONC with minimal examples only.

### Step 2.1 — Add "Configuration alternatives" (or equivalent) to config docs ✅

- **Done.** Added "Configuration alternatives" section in `docs/config/README.md` with JSON/JSONC support statement, JSONC features list, minimal JSON and JSONC examples (globals + one project), and pointer to reference.yaml and examples/hal.config.yaml.
- In `docs/config/README.md` (or a dedicated subpage linked from there), add a section such as **"Configuration alternatives"** or **"Other config formats"**.
- Content must include:
  - Statement that `hal.config.json` and `hal.config.jsonc` are supported alongside `hal.config.yaml`; runtime behavior is unchanged.
  - List of **JSONC features** supported: single-line `//` comments, block `/* */` comments, trailing commas. Explicitly state what is **not** supported (e.g. unquoted keys, single quotes).
  - **One small example for JSON:** globals + one hypothetical project only (main config). No full multi-project example.
  - **One small example for JSONC:** same structure, with a few comments and trailing commas to demonstrate syntax. Main config only; no `hal.config.local` example in this section.
- Point users to `docs/config/reference.yaml` (and optionally `examples/hal.config.yaml`) for full structure; users can convert from YAML (e.g. with AI) if they need JSON/JSONC for a larger setup.

### Step 2.2 — Update config index to be YAML-first ✅

- **Done.** Config files subsection now leads with YAML, primary example is YAML, links to reference.yaml and examples/hal.config.yaml; Configuration alternatives covers JSON/JSONC.
- In `docs/config/README.md`, make YAML the primary format: first example in "Config files" / "hal.config.*" should be YAML (or link to `reference.yaml` and `examples/hal.config.yaml`). Remove or replace the current large JSON snippet with YAML or a short pointer to the reference/example.
- Ensure the "Config files" subsection and table still mention all three formats but lead with YAML and link to the new Configuration alternatives section for JSON/JSONC details.

---

## Phase 3 — Convert all config snippets in docs to YAML

**Goal:** Every config snippet in `docs/` uses YAML.

### Step 3.1 — `docs/config/README.md` ✅

- **Done.** Main and local config examples are YAML; Configuration alternatives has minimal JSON/JSONC.
- Replace any remaining ` ```json ` code blocks with ` ```yaml ` and equivalent YAML (e.g. main config example, local config example). Use consistent style (e.g. 2-space indent). Update links to point to `examples/hal.config.yaml` and `reference.yaml` where appropriate.

### Step 3.2 — `docs/config` subpages ✅

- **Done.** commands, context, logging, rate-limit READMEs converted to YAML.
- **docs/config/commands/README.md** — Convert JSON snippets to YAML.
- **docs/config/context/README.md** — Convert JSON snippet(s) to YAML.
- **docs/config/logging/README.md** — Convert JSON snippet to YAML.
- **docs/config/rate-limit/README.md** — Convert JSON snippet to YAML.

### Step 3.3 — `docs/engines/README.md` and per-engine docs ✅

- **Done.** engines/README and all six engine READMEs (claude, codex, copilot, cursor, opencode, antigravity) use YAML snippets.
- **docs/engines/README.md** — Convert any `hal.config.json` snippets to YAML.
- **docs/engines/claude/README.md**, **codex/README.md**, **copilot/README.md**, **cursor/README.md**, **opencode/README.md**, **antigravity/README.md** — Convert config examples to YAML.

### Step 3.4 — Other docs ✅

- **Done.** telegram, voice READMEs converted to YAML; custom-commands table cell updated to "config file (e.g. hal.config.yaml)"; skills has no config snippets.
- **docs/telegram/README.md** — Convert all config examples to YAML.
- **docs/voice/README.md** — Convert config examples to YAML.
- **docs/custom-commands/README.md** — If it contains config snippets, convert to YAML.
- **docs/skills/README.md** — If it references or shows config, use YAML.

---

## Phase 4 — README and doc links

**Goal:** Root README and doc index use YAML and correct links.

### Step 4.1 — Root `README.md` ✅

- **Done.** Quick Start and Configuration sections use "config file" / hal.config.yaml; minimal example is YAML; JSONC collapsible replaced with pointer to Configuration alternatives.
- Replace references to `hal.config.json` with `hal.config.yaml` (or "config file" with a link to the YAML example) in setup and configuration steps.
- Replace the JSONC example link/mention with a pointer to the Configuration alternatives section (e.g. "YAML is the default; JSON and JSONC are also supported — see [Configuration](docs/config/README.md).").
- Ensure any config snippet in README is YAML. Update links to `examples/hal.config.yaml` and/or `docs/config/reference.yaml` as appropriate.

### Step 4.2 — `docs/README.md` and cross-links ✅

- **Done.** docs/README Configuration row now links to reference.yaml.
- Ensure the docs index links to the config section and, if useful, to `docs/config/reference.yaml` as the full key reference.
- Scan for broken links (e.g. old `examples/hal.config.jsonc`) and fix them to point to YAML examples or the config docs.

---

## Phase 5 — Update `.agents/skills`

**Goal:** Skills reference YAML-only examples and the docs skill encodes the "update reference" rule.

### Step 5.1 — `refresh_models` skill ✅

- **Done.** Step 1 now references only examples/hal.config.yaml (YAML).
- In `.agents/skills/refresh_models/SKILL.md`, update the "Identify the example config" step: reference **only** YAML example(s) in `examples/` (e.g. `examples/hal.config.yaml`). Remove or replace references to `examples/hal.config.jsonc` or `examples/_hal.config.yaml` so the skill expects a single primary example path: `examples/hal.config.yaml`.

### Step 5.2 — Docs skill ✅

- **Done.** Added "Config examples and reference" subsection: YAML-first, reference.yaml, examples/hal.config.yaml, and rule to update reference.yaml + doc page when adding/changing config keys.
- In `.agents/skills/docs/SKILL.md`, add a subsection (e.g. **Config examples and reference**) that states:
  - Config examples in the repo are **YAML-first**; the canonical full key reference is **`docs/config/reference.yaml`** (all keys, with comments linking to doc pages). The short copy-paste example is **`examples/hal.config.yaml`**.
  - **When adding or changing a config key:** update `docs/config/reference.yaml` and the relevant doc page (e.g. Context, Commands, Engines, Rate limit, Logging) so the reference stays the single source of truth.
- Keep existing guidance (README vs docs/, structure, cross-linking, external links). Ensure the new subsection is discoverable from the "How to manage the documentation" area.

---

## Phase 6 — Final review

**Goal:** No broken links, consistent format, acceptance criteria met.

### Step 6.1 — Links and paths ✅

- **Done.** No remaining broken links to hal.config.jsonc or _hal.config.yaml in active README/docs/skills; only historical references in tasks/completed and vibed.
- Grep or manually check for remaining references to `hal.config.jsonc`, `_hal.config.yaml`, or `examples/...json` in README, `docs/`, and `.agents/skills`. Replace or remove as needed.
- Verify relative links from `docs/config/reference.yaml` to sibling and parent docs (context, commands, logging, rate-limit, engines) work when browsed from GitHub.

### Step 6.2 — Acceptance criteria checklist

- [x] Every config snippet in README.md, `docs/`, and any `.agents/skills` that show config uses YAML.
- [x] `docs/config/reference.yaml` exists with all supported keys and comments linking to doc pages; `examples/hal.config.yaml` is a shorter subset.
- [x] Config docs include a "Configuration alternatives" section with JSON/JSONC support, feature list, and one small example (globals + one project) per format.
- [x] `examples/` contains only YAML; any JSON/JSONC example files removed; links point to YAML.
- [x] README, `examples/`, and `docs/` reviewed for broken links and format; fixes applied.
- [x] `.agents/skills` (refresh_models, docs) reference correct paths and format; docs skill states YAML-first, reference.yaml, and the rule to update reference.yaml + doc page when adding/changing config keys.

---

## File summary

| Area | Action |
|------|--------|
| `docs/config/reference.yaml` | **Create** — full key reference with doc links |
| `docs/config/README.md` | **Edit** — YAML-first examples, add Configuration alternatives, convert snippets |
| `docs/config/commands/README.md` | **Edit** — convert snippets to YAML |
| `docs/config/context/README.md` | **Edit** — convert to YAML |
| `docs/config/logging/README.md` | **Edit** — convert to YAML |
| `docs/config/rate-limit/README.md` | **Edit** — convert to YAML |
| `docs/engines/README.md` + engine pages | **Edit** — convert to YAML |
| `docs/telegram/README.md`, `docs/voice/README.md`, etc. | **Edit** — convert to YAML |
| `README.md` | **Edit** — YAML, link to config docs and examples |
| `examples/hal.config.yaml` | **Create or update** — short subset |
| `examples/hal.config.jsonc` (or `.json` / `_hal.config.*`) | **Remove** |
| `.agents/skills/refresh_models/SKILL.md` | **Edit** — YAML example path only |
| `.agents/skills/docs/SKILL.md` | **Edit** — YAML-first, reference.yaml, update rule |
