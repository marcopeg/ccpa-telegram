# Plan — 066 Bar substitution in .md skills and crons

## Phase 1 — Design

- Define bar substitution syntax (`|key|`) and resolution rules (context map, env fallback optional, unresolved stays literal).
- Identify where context maps are created for skills and cron executions.

## Phase 2 — Implementation

- Add a shared helper for bar substitution.
- Apply substitution to SKILL.md prompt bodies just before dispatch.
- Apply substitution to cron `.md` prompt bodies after context vars are built.
- Preserve existing `${VAR}` frontmatter substitution logic for crons.

## Phase 3 — Verification

- Add tests for skill prompt substitution.
- Add tests for cron `.md` prompt substitution.
- Verify missing keys remain unchanged.

## Phase 4 — Docs

- Update skills/crons docs to describe bar substitution once implemented.
