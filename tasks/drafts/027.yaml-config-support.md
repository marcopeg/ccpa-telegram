# 027 — YAML config support

## Status

draft

## Summary

Add support for YAML configuration files in addition to JSON.

## Requirements

- Load configuration from `hal.config.yaml`.
- Load local overrides from `hal.config.loca.yaml`.
- Preserve existing JSON config behavior and precedence.

## Notes

- Keep backward compatibility with existing `hal.config.json` workflows.
